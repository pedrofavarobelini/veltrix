# PedroCore IA — Status Atual

## Versão atual

V2 — Multi-provider completo inicial

## Status

ZIP revisado e pronto para instalação/teste.

## Providers

- Mock: funcional.
- Gemini: estrutura pronta, depende de `GEMINI_API_KEY`.
- OpenAI: estrutura pronta, depende de `OPENAI_API_KEY`.
- Claude: estrutura pronta, depende de `ANTHROPIC_API_KEY`.
- DeepSeek: estrutura pronta, depende de `DEEPSEEK_API_KEY`.
- Grok: estrutura pronta, depende de `XAI_API_KEY`.

## Próxima etapa

Rodar testes locais, configurar as chaves que o usuário possuir e validar os providers reais.
